# MLUONET - 企业管理系统

[![版本](https://img.shields.io/badge/version-1.2.0-blue.svg)](https://github.com/mluonet/mluonet)
[![许可证](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)
[![构建状态](https://img.shields.io/badge/build-passing-brightgreen.svg)](https://github.com/mluonet/mluonet)

## 🚀 包含企业级预水合清理工具

本项目现在包含了一个**生产就绪的预水合清理工具**，专门解决Next.js应用中浏览器扩展导致的hydration警告问题。

### 📦 独立包发布

预水合清理工具已作为独立包发布：

```bash
npm install @mluonet/pre-hydration-cleanup
```

- 📚 **完整文档**: [packages/pre-hydration-cleanup/README.md](packages/pre-hydration-cleanup/README.md)
- 🔧 **使用示例**: [docs/USAGE_EXAMPLES.md](docs/USAGE_EXAMPLES.md)
- 🛡️ **CSP安全指南**: [docs/CSP_SECURITY_GUIDE.md](docs/CSP_SECURITY_GUIDE.md)
- 🚨 **故障排除**: [docs/TROUBLESHOOTING_GUIDE.md](docs/TROUBLESHOOTING_GUIDE.md)

## 项目概述

MLUONET是一个现代化的企业管理系统，提供完整的业务管理解决方案，包括报价、采购、发票、装箱单等核心功能。

## 主要功能

### 🎨 主题管理系统
- **多主题支持**: 支持 `colorful` 和 `classic` 两种按钮主题
- **深色模式**: 完整的深色模式支持，自动适配系统偏好
- **CSS变量系统**: 使用CSS变量统一管理颜色和样式
- **动态主题切换**: 实时主题切换，无需刷新页面
- **主题持久化**: 主题设置自动保存到localStorage
- **响应式设计**: 完美适配移动端和桌面端
- **主题组件**: 提供多种主题切换组件（按钮、下拉菜单、紧凑模式）
- **CSS级联优化**: 解决硬编码类与动态主题管理的冲突问题
- **调试工具**: 提供完整的主题调试和测试工具

### 核心业务模块
- **报价管理**: 创建和管理客户报价单
  - **Notes自定义排序**: 支持拖拽排序和显示控制
  - **默认排序**: Delivery time → Price based on → Delivery terms → Payment term → Validity
  - **完整Notes类型**: 包含9种Notes类型，支持质量条款、保修条款等
  - **序号开关合并**: 序号圆圈同时作为开关，点击切换显示/隐藏状态
  - **卡片式布局**: 单行展示，Label — 内容格式，视觉更整齐
  - **展开态强化**: 展开时背景色变化，箭头旋转，视觉层次清晰
  - **收缩态标签**: 显示选中选项的标签提示，无需展开即可预览
  - **分组选项**: 常用选项和其他选项分组展示，减少滚动压力
  - **搜索功能**: 支持选项搜索，快速定位所需内容
  - **拖拽编辑分离**: 拖拽句柄与编辑区域完全分离，避免冲突
  - **内联编辑**: 点击文本进入编辑模式，Enter保存Esc取消，支持所有Notes类型
  - **编辑状态提示**: 编辑时卡片高亮显示，拖拽句柄自动隐藏
  - **错误处理**: 自动保存失败时自动清理存储空间，PDF生成时处理未定义数据
  - **数据净化**: 统一的数据净化函数，确保PDF生成时数据格式正确
  - **存储优化**: 精简数据存储，避免localStorage配额超限
  - **进度提示**: PDF预览和生成时显示独立的进度条和加载状态
  - **性能优化**: 避免重复数据净化，减少不必要的处理开销
  - **批量操作**: 全选/全不选/仅常用/恢复默认顺序
  - **双语模板**: EXW工厂交货/FOB离岸价/CIF到岸价等一键套用
  - **内联选择器**: Payment Terms和Delivery Terms支持下拉选择
  - **空值防御**: 可见但内容为空的条款会显示提醒
  - **配置面板**: 可选择显示/隐藏特定Notes类型
  - **Excel导出功能**: 支持报价单和销售确认的Excel导出，包含完整的商品信息、费用明细和合同条款
- **采购管理**: 处理供应商采购订单
- **发票管理**: 生成和管理发票
- **装箱单管理**: 创建详细的装箱清单
- **邮件系统**: 集成AI邮件助手功能
  - **ChatGPT风格界面**: 现代化的聊天界面，节省空间
  - **邮件编写**: 支持多种语言和风格的邮件生成
  - **邮件回复**: 基于原始邮件和回复草稿生成优化回复
  - **多语言支持**: 支持英文、中文和双语模式
  - **多种风格**: 正式、专业、友好、简洁、详细、非正式、激励等风格
  - **智能诊断**: 自动检测连接问题并提供解决方案
  - **健康检查**: 实时监控API连接状态

### Dashboard 智能管理
- **模块化设计**: 清晰的模块按钮和权限控制
- **实时文档管理**: 支持时间筛选、类型筛选和搜索功能
- **智能预加载**: 悬停预加载和权限动态预加载
- **多源权限容错**: 支持 Store、Session、本地缓存三重权限源
- **搜索高亮**: 支持文档编号、客户名称等关键词搜索和高亮显示
- **响应式布局**: 完美适配移动端和桌面端

### 404页面娱乐功能
- **五子棋游戏**: 经典的五子棋对战游戏
- **2048游戏**: 数字合并游戏，支持AI推演功能

## Dashboard 页面优化

### 架构重构
- **模块化拆分**: 将大型组件拆分为独立的功能模块
  - `ModuleButton`: 模块按钮组件，支持徽章显示和快捷键
  - `RecentDocumentsList`: 最近文档列表，支持搜索和筛选
  - `dashboardModules.ts`: 模块配置常量
  - `documentCounts.ts`: 统一文档计数工具
  - `mapPermissions.ts`: 权限映射工具，使用 Map 加速判断

### 性能优化
- **统一缓存读取**: 合并所有文档类型的计数逻辑，避免重复解析
- **权限判断优化**: 使用 Map 数据结构加速权限判断
- **安全本地存储**: 封装 `getSafeLocalStorage` 工具，避免 SSR 错误
- **智能预加载**: 根据权限动态预加载组件，减少不必要的资源加载

### 用户体验提升
- **搜索功能**: 支持文档编号、客户名称、供应商名称搜索
- **关键词高亮**: 搜索结果中的匹配词自动高亮显示
- **空状态优化**: 根据搜索条件和筛选器显示相应的空状态提示
- **响应式筛选器**: 支持展开/收起筛选器，节省屏幕空间

### 代码质量改进
- **类型安全**: 完整的 TypeScript 类型定义
- **错误处理**: 完善的错误边界和异常处理
- **代码复用**: 提取公共逻辑到工具函数
- **可维护性**: 清晰的代码结构和注释

## 稳定性防回归机制

### 🛡️ 完整防护体系

项目建立了完整的"稳定引用 + 原子订阅"防回归机制，确保代码质量和性能稳定性：

#### 1. 循环哨兵监控
- **文件**: `src/debug/useRenderLoopGuard.ts`
- **功能**: 开发环境监控组件渲染次数
- **使用**: 在容器组件中调用 `useRenderLoopGuard('ComponentName', 80)`

#### 2. 稳定性测试
- **文件**: `src/features/purchase/state/__tests__/purchase.selectors.stability.test.ts`
- **覆盖**: 所有关键派生Hook的引用稳定性测试
- **验证**: 两次渲染引用相等，依赖变化时引用更新

#### 3. ESLint规则守护
- **文件**: `.eslintrc.json`
- **规则**: 
  - `no-new-object`: 禁止在selector中创建新对象
  - `no-array-constructor`: 禁止在selector中创建新数组
  - `@typescript-eslint/no-object-literal-type-assertion`: 禁止对象字面量断言

#### 4. 发版前自检脚本
- **文件**: `scripts/pre-release-check.js`
- **检查项**:
  - Selector中的时间戳生成
  - useEffect依赖中的现拼对象
  - 对象返回selector的shallow使用
  - useEffect依赖中的匿名函数
  - Selector中的Math.random
  - 稳定性测试通过

#### 5. 防回归指南
- **文件**: `docs/STABILITY_GUARDRAILS.md`
- **内容**: 6个复发点检查清单、最佳实践、故障排除

### 🔧 使用方式

#### 开发时
```bash
# 运行稳定性测试
npm test -- --testPathPattern=purchase.selectors.stability.test.ts

# 运行ESLint检查
npm run lint

# 在组件中使用循环哨兵
import { useRenderLoopGuard } from '@/debug/useRenderLoopGuard';
useRenderLoopGuard('ComponentName');
```

#### 发版前
```bash
# 运行完整自检
npm run check:selectors

# 或运行完整预发布检查
npm run pre-release
```

### 📋 6个"复发点"防护

1. **Selector中的时间戳生成** ❌
   ```ts
   // 错误: useStore(s => Date.now())
   // 正确: 传参或在触发时生成
   ```

2. **Selector中的map/filter返回新数组** ❌
   ```ts
   // 错误: useStore(s => s.items.filter(...))
   // 正确: 原子订阅 + useMemo
   ```

3. **useEffect依赖中的现拼对象** ❌
   ```ts
   // 错误: useEffect(() => {}, [{ id: 1 }])
   // 正确: useMemo稳定化
   ```

4. **对象返回selector未使用shallow** ❌
   ```ts
   // 错误: useStore(s => ({ a: s.a, b: s.b }))
   // 正确: useStore(s => ({ a: s.a, b: s.b }), shallow)
   ```

5. **PDF相关selector中的易变数据** ❌
   ```ts
   // 错误: selector中生成时间戳
   // 正确: 在触发动作时生成
   ```

6. **Selector中的字符串拼接** ❌
   ```ts
   // 错误: useStore(s => `$${s.amount}`)
   // 正确: 在展示层格式化
   ```

### 🎯 最佳实践

#### 原子订阅原则
```ts
// ✅ 原子订阅
const items = useStore(s => s.items);
const currency = useStore(s => s.currency);

// ❌ 复合订阅
const { items, currency } = useStore(s => ({ items: s.items, currency: s.currency }));
```

#### 派生数据使用useMemo
```ts
const useTotals = () => {
  const items = useStore(s => s.items);
  return useMemo(() => {
    return items.reduce((sum, item) => sum + item.price * item.qty, 0);
  }, [items]);
};
```

#### 复杂对象使用shallow
```ts
const { buyer, seller } = useStore(s => ({
  buyer: s.buyer,
  seller: s.seller
}), shallow);
```

## 2048游戏推演功能

### 功能特点
- **AI自动推演**: 使用Expectimax算法和蒙特卡洛树搜索进行智能推演
- **随机演示**: 使用随机方向移动进行调试和演示
- **实时统计**: 显示推演步数、模拟次数、当前分数等
- **速度控制**: 支持快速、正常、慢速、极慢四种推演速度
- **手动干预**: 可随时暂停推演并进行手动操作
- **撤销功能**: 支持撤销到上一步状态
- **单步推演**: 可以手动控制AI走一步

### 使用方法
1. **开始推演**: 点击"开始推演"按钮，AI将自动进行游戏
2. **暂停推演**: 点击"暂停推演"按钮停止自动推演
3. **单步推演**: 点击"单步推演"按钮，AI走一步
4. **随机演示**: 点击"随机演示"按钮，使用随机方向移动
5. **撤销操作**: 点击"撤销"按钮回到上一步
6. **速度调节**: 使用下拉菜单调整推演速度
7. **手动干预**: 在推演过程中可以随时进行手动操作

### 算法说明
- **Expectimax算法**: 2层深度搜索，支持Alpha-Beta剪枝优化
- **蒙特卡洛树搜索**: 备选算法，支持100次随机模拟
- **综合评估函数**: 基于空格数量、合并潜力、单调性、平滑度、角落策略和死路惩罚
- **状态管理**: 完整的撤销栈和状态快照管理，使用JSON深拷贝确保状态隔离

### 技术实现
- **React Hooks**: 使用useState、useEffect、useRef管理状态
- **算法优化**: 避免阻塞主线程的计算密集型操作
- **响应式设计**: 适配移动端和桌面端
- **性能监控**: 实时显示推演统计信息

## 技术栈

- **前端框架**: Next.js 14 (App Router)
- **UI组件**: Tailwind CSS
- **图标库**: Lucide React
- **状态管理**: React Hooks + Zustand
- **主题管理**: 自定义主题系统 + CSS变量
- **部署平台**: Vercel

## 开发环境

```bash
# 安装依赖
npm install

# 启动开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm start
```

## 项目结构

```
mluonet/
├── src/
│   ├── app/                 # Next.js App Router页面
│   │   └── dashboard/       # Dashboard 页面
│   ├── components/          # React组件
│   │   ├── dashboard/       # Dashboard 相关组件
│   │   │   ├── ModuleButton.tsx
│   │   │   └── RecentDocumentsList.tsx
│   │   ├── ThemeToggle.tsx  # 主题切换组件
│   │   └── ThemeProvider.tsx # 主题提供者
│   ├── constants/           # 常量配置
│   │   ├── dashboardModules.ts
│   │   └── colorMap.ts      # 颜色映射配置
│   ├── hooks/               # React Hooks
│   │   ├── useThemeManager.ts # 主题管理Hook
│   │   └── useThemeSettings.ts # 旧版主题设置Hook
│   ├── lib/                 # 工具库和配置
│   ├── types/               # TypeScript类型定义
│   │   └── permissions.ts   # 权限相关类型
│   └── utils/               # 工具函数
│       ├── documentCounts.ts
│       ├── mapPermissions.ts
│       ├── themeUtils.ts    # 主题管理工具类
│       └── themeStyles.ts   # 主题样式工具类
├── public/                  # 静态资源
└── scripts/                 # 构建脚本
```

## 部署

项目已配置为Vercel部署，包含以下优化：

- **字体优化**: 中文字体压缩和预加载
- **图片优化**: Logo和图标资源优化
- **性能监控**: 实时性能数据收集
- **错误处理**: 完善的错误边界和404页面

### 邮件助手配置

邮件助手模块需要配置DeepSeek API密钥才能正常工作：

#### 1. 获取API密钥
1. 访问 [DeepSeek Console](https://platform.deepseek.com/)
2. 注册或登录账户
3. 进入 **API Keys** 页面
4. 创建新的API密钥

#### 2. 配置环境变量
在Vercel项目设置中添加环境变量：

| 变量名 | 值 | 环境 |
|--------|-----|------|
| `DEEPSEEK_API_KEY` | `sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx` | Production, Preview, Development |

#### 3. 验证配置
访问 `/api/health` 端点检查配置状态，或查看 [MAIL_CONNECTION_FIX.md](MAIL_CONNECTION_FIX.md) 获取详细解决方案。

## 更新日志

### 最新版本 V1.3.2 (2025-01-08) - 导入导出功能全面优化版本

**🔧 关键修复**
- ✅ **导出功能逻辑修复**: 解决ExportModal中所有按钮都调用同一个函数的问题
  - **问题描述**: ExportModal中的current、all、filtered、selected按钮都调用同一个函数，无法区分不同的导出类型
  - **修复方案**: 重构导出逻辑，为每种导出类型提供独立的处理逻辑
  - **技术细节**: 在 `src/app/history/ExportModal.tsx` 中实现完整的switch-case逻辑
    - `current`: 导出当前选项卡的所有数据
    - `all`: 导出所有类型的数据（调用fullExport函数）
    - `filtered`: 导出筛选结果数据
    - `selected`: 导出选中的数据
  - **影响范围**: 影响所有历史数据的导出功能，提供完整的导出选项
  - **向后兼容**: 修复不影响现有功能，仅改善导出功能的完整性

- ✅ **导入数据类型分离**: 解决confirmation类型数据被错误处理为quotation的问题
  - **问题描述**: 导入时confirmation类型的数据被当作quotation处理，导致数据分类错误
  - **修复方案**: 为confirmation类型提供独立的导入处理逻辑
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中分离quotation和confirmation的导入逻辑
  - **影响范围**: 影响订单确认数据的导入，确保数据正确分类
  - **向后兼容**: 修复不影响现有功能，仅改善数据分类的准确性

- ✅ **冗余代码清理**: 识别并标记未使用的导出函数
  - **问题描述**: 各个历史模块中定义了导出函数但未被使用，造成代码冗余
  - **发现**: `exportQuotationHistory`、`exportPurchaseHistory`、`exportInvoiceHistory`、`exportPackingHistory` 函数未被引用
  - **建议**: 这些函数可以安全移除，因为统一使用 `historyImportExport.ts` 中的导出逻辑
  - **影响范围**: 代码维护性提升，减少冗余代码

- ✅ **全量导出功能完善**: 修复全量导出时遗漏confirmation类型数据的问题
  - **问题描述**: `fullExport` 函数没有包含 `confirmation` 类型的数据，导致全量导出不完整
  - **修复方案**: 修改全量导出逻辑，正确分离和包含所有数据类型
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中修复 `fullExport` 函数
    - 正确分离 quotation 和 confirmation 数据
    - 包含所有5种数据类型：quotation、confirmation、purchase、invoice、packing
    - 更新导出统计信息，显示每种类型的数量
  - **影响范围**: 确保全量导出包含所有历史数据，提供完整的数据备份
  - **向后兼容**: 修复不影响现有功能，仅改善全量导出的完整性

- ✅ **导入导出调试增强**: 添加详细的调试信息帮助诊断数据问题
  - **问题描述**: 用户反馈采购单数据未能导入，需要更详细的调试信息来诊断问题
  - **修复方案**: 在导入和导出过程中添加详细的调试日志
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中添加调试信息
    - 全量导出时显示每种类型的数据统计
    - 导入时显示每条数据的详细信息和类型检测过程
    - 帮助用户和开发者快速定位数据问题
  - **影响范围**: 改善调试体验，便于问题排查和数据验证
  - **向后兼容**: 不影响现有功能，仅增加调试信息

- ✅ **采购单导入问题修复**: 解决采购单数据无法导入的问题
  - **问题描述**: 采购单数据在导出时缺少type字段，导致导入时无法识别其类型
  - **根本原因**: PurchaseHistory接口没有type字段，而其他历史记录类型都有
  - **修复方案**: 在导出时为采购单、发票、装箱单数据添加type字段，并增强导入时的智能识别
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 全量导出时自动为采购单、发票、装箱单数据添加type字段
    - 导入时通过字段特征智能识别没有type字段的数据
    - 支持通过supplierName、consigneeName、invoiceNo等字段识别数据类型
  - **影响范围**: 确保所有类型的数据都能正确导出和导入
  - **向后兼容**: 完全兼容现有数据，同时支持新的智能识别功能

- ✅ **旧版导出格式兼容**: 增强对旧版导出数据格式的兼容性
  - **问题描述**: 用户反馈旧版导出的数据无法正确导入，数据格式为以类型名称为键的对象结构
  - **修复方案**: 增强智能识别功能，支持通过ID字段识别数据类型
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 支持通过ID字段识别数据类型（quotation、confirmation、purchase、invoice、packing）
    - 更新所有类型的过滤条件，支持ID字段识别
    - 兼容多种数据格式：标准格式、智能识别格式、旧版格式
  - **影响范围**: 大幅提升数据格式兼容性，支持更多历史导出文件
  - **向后兼容**: 完全兼容所有现有数据格式，无破坏性变更

- ✅ **导入数据格式修复**: 修复导入数据缺失字段导致的组件渲染错误
  - **问题描述**: 导入的数据缺少必要字段（如totalAmount、currency、createdAt等），导致PackingHistoryTab组件出现"Cannot read properties of undefined"错误
  - **修复方案**: 增强processDocumentData函数，为导入的数据添加必要的默认值
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 为所有导入数据添加默认ID、类型、时间戳等必要字段
    - 确保金额字段（totalAmount、currency）有默认值
    - 确保基本字段（customerName、supplierName等）有默认值
    - 确保编号字段（quotationNo、invoiceNo等）有默认值
    - 确保items数组有默认值
  - **影响范围**: 解决导入数据后组件渲染错误，提升数据导入的稳定性
  - **向后兼容**: 完全兼容所有现有数据格式，自动补充缺失字段

- ✅ **分组数据结构导入修复**: 修复分组导出数据结构的导入逻辑
  - **问题描述**: 用户反馈导入时每种类型只有1条记录，实际应该是每种类型包含多条记录的分组数据结构
  - **修复方案**: 重构导入逻辑，正确处理以类型名称为键的分组数据结构
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 识别ID为类型名称的对象（如{id: 'quotation', data: [...]}）
    - 从分组对象中提取实际数据数组（支持data、items、records等字段名）
    - 为每种类型添加详细的数据提取日志
    - 保持对旧格式的完全兼容性
  - **影响范围**: 正确导入分组数据结构，确保每种类型的所有记录都能被导入
  - **向后兼容**: 完全兼容所有现有数据格式，包括分组格式和数组格式

- ✅ **数字键对象结构导入修复**: 修复以数字为键的对象数据结构的导入逻辑
  - **问题描述**: 用户反馈导入时每种类型仍然只有1条记录，实际数据结构是以数字为键的对象（如{0: {...}, 1: {...}, 2: {...}}）
  - **修复方案**: 增强数据提取逻辑，识别并正确处理以数字为键的对象结构
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 识别以数字为键的对象结构（如{0: {...}, 1: {...}, 2: {...}}）
    - 按数字键排序并提取所有记录
    - 为每种类型添加数字键提取的详细日志
    - 保持对所有现有格式的完全兼容性
  - **影响范围**: 正确导入以数字为键的对象结构，确保所有记录都能被正确提取和导入
  - **向后兼容**: 完全兼容所有现有数据格式，包括数字键格式、数组格式和分组格式

- ✅ **导入功能完全修复**: 成功修复所有导入问题，实现完整的数据导入功能
  - **问题描述**: 用户反馈导入功能不完整，无法正确导入所有类型的数据
  - **修复方案**: 通过多轮调试和修复，完全解决了数据导入的所有问题
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中实现
    - 修复字体文件404错误
    - 增强智能识别功能，支持多种数据格式
    - 修复分组数据结构导入逻辑
    - 修复数字键对象结构导入逻辑
    - 增强数据字段默认值处理，确保组件正常渲染
    - 支持通过ID字段、字段特征等多种方式识别数据类型
  - **测试结果**: 成功导入215条记录（报价单145条、订单确认27条、采购单29条、发票6条、装箱单8条）
  - **影响范围**: 完全解决了数据导入功能，支持所有历史导出文件的导入
  - **向后兼容**: 完全兼容所有现有数据格式，无破坏性变更

- ✅ **历史标签页渲染错误修复**: 修复装箱单和发票标签页打开时的渲染错误
  - **问题描述**: 用户反馈装箱单和发票标签页打开时出现"Cannot read properties of undefined (reading 'toLocaleString')"错误
  - **根本原因**: 历史数据中某些记录的 `totalAmount` 或 `currency` 字段为 `undefined`，导致渲染时调用 `toLocaleString()` 失败
  - **修复方案**: 在数据获取层面添加数据清理功能，确保所有字段都有正确的默认值
  - **技术细节**: 
    - 在 `src/utils/packingHistory.ts` 中实现 `sanitizePackingHistoryItem` 函数
    - 在 `src/utils/invoiceHistory.ts` 中实现 `sanitizeInvoiceHistoryItem` 函数
    - 在 `getPackingHistory()` 和 `getInvoiceHistory()` 中应用数据清理
    - 确保所有金额字段都有默认值（totalAmount: 0, currency: 'USD'）
    - 确保所有字符串字段都有默认值（空字符串）
    - 确保所有时间字段都有默认值（当前时间）
  - **影响范围**: 解决所有历史标签页的渲染错误，确保数据完整性
  - **向后兼容**: 完全兼容所有现有数据格式，自动补充缺失字段

### 历史版本 V1.3.1 (2025-01-08) - 控制台错误修复版本

**🐛 关键修复**
- ✅ **字体文件404错误修复**: 解决压缩字体文件路径错误导致的404错误
  - **问题描述**: 字体缓存配置中使用了错误的路径 `/fonts/compressed/`，但实际文件在 `/fonts/` 目录下
  - **修复方案**: 修正 `src/utils/fontCache.ts` 中的字体文件路径配置
  - **技术细节**: 将所有 `/fonts/compressed/NotoSansSC-*.ttf.gz` 路径修正为 `/fonts/NotoSansSC-*.ttf.gz`
  - **影响范围**: 影响PDF生成时的字体加载，提升字体加载成功率
  - **向后兼容**: 修复不影响现有功能，仅改善字体加载的稳定性

- ✅ **导出功能修复**: 解决历史数据导出时"没有数据可导出"的错误
  - **问题描述**: 导出功能直接调用历史数据获取函数，但可能返回空数组导致导出失败
  - **根本原因**: 历史数据获取函数使用了安全的localStorage读取，但导出逻辑没有处理空数据情况
  - **修复方案**: 优化导出逻辑，确保正确处理空数据情况并提供更好的错误提示
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中改进数据获取和错误处理逻辑
  - **影响范围**: 影响所有历史数据的导出功能，提升导出成功率
  - **向后兼容**: 修复不影响现有功能，仅改善导出功能的稳定性

- ✅ **导入功能修复**: 解决历史数据导入时数据类型检测失败的问题
  - **问题描述**: 导入功能期望数据有 `documentType` 字段，但实际历史数据使用 `type` 字段
  - **根本原因**: 导入逻辑与历史数据实际结构不匹配，导致数据类型检测失败
  - **修复方案**: 修改导入逻辑以支持多种数据类型字段格式
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中改进数据类型检测逻辑，支持 `documentType` 和 `type` 两种字段
  - **影响范围**: 影响所有历史数据的导入功能，提升导入成功率
  - **向后兼容**: 修复不影响现有功能，仅改善导入功能的兼容性

- ✅ **导入数据解析优化**: 解决导入时显示"包含 undefined 条记录"的问题
  - **问题描述**: 导入数据解析时显示"包含 undefined 条记录"，无法正确显示数据量
  - **根本原因**: 导入数据可能不是数组格式，直接访问 `.length` 属性导致 undefined
  - **修复方案**: 改进数据解析逻辑，提供更详细的调试信息和错误处理
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中添加数据类型检查和详细的错误提示
  - **影响范围**: 改善导入功能的调试体验和错误提示
  - **向后兼容**: 修复不影响现有功能，仅改善错误处理和调试信息

- ✅ **导入数据格式兼容性增强**: 解决对象格式数据导入失败的问题
  - **问题描述**: 导入功能只支持数组格式，遇到对象格式数据时显示"期望数组，实际得到 object 类型"错误
  - **根本原因**: 导入逻辑过于严格，只接受数组格式，不支持常见的对象包装格式
  - **修复方案**: 增强数据格式兼容性，支持多种常见的数据结构
  - **技术细节**: 在 `src/utils/historyImportExport.ts` 中添加智能数据格式检测和转换逻辑
    - 支持直接数组格式：`[{...}, {...}]`
    - 支持对象包装格式：`{data: [...], items: [...], records: [...], history: [...]}`
    - 支持ID键值对象格式：`{id1: {...}, id2: {...}}`
  - **影响范围**: 大幅提升导入功能的兼容性，支持更多数据格式
  - **向后兼容**: 完全兼容现有数组格式，同时支持新的对象格式

### 历史版本 V1.3.0 (2025-01-08) - Notes分页最大化优化版本

**🔧 关键优化**
- ✅ **Notes条款分页最大化优化**: 最大化第一页空间利用率，让更多条款在第一页显示
  - **问题描述**: 即使经过多次优化，第一页仍有大量空白空间，可以显示更多条款
  - **优化方案**: 最小化预留空间和间距估算，最大化第一页空间利用率
  - **技术细节**: 
    - 将分页边界从`pageHeight - y - 15`进一步优化为`pageHeight - y - 10`，只预留10mm给页码和底部边距
    - 最小化条款高度估算中的额外间距，从`2 + 内容高度 + 2`改为`1 + 内容高度 + 1`
    - 使用最精确的可用空间计算，最大化第一页空间利用率
  - **优化效果**: 
    - 第一页空间得到最大化利用，可以显示更多条款
    - 充分利用第一页的每一寸空间，减少不必要的换页
    - 让PDF显示更加紧凑和高效
  - **影响范围**: 影响报价模块和订单确认模块PDF生成，最大化空间利用率
  - **向后兼容**: 优化不影响现有功能，仅改善分页逻辑的精确性

### 历史版本 V1.2.9 (2025-01-08) - Notes分页最终优化版本

**🔧 关键优化**
- ✅ **Notes条款分页阈值最终优化**: 进一步优化分页检查的保守程度，让第一页空间得到最大程度利用
  - **问题描述**: 即使经过第一次优化，分页检查仍然过于保守，第2条后就将第3条移到了第二页
  - **优化方案**: 进一步调整分页检查的阈值和空间估算逻辑，让第一页空间得到最大程度利用
  - **技术细节**: 
    - 将分页边界从`pageHeight - y - 30`进一步优化为`pageHeight - y - 15`，只预留15mm给页码和底部边距
    - 进一步减少条款高度估算中的额外间距，从`3 + 内容高度 + 3`改为`2 + 内容高度 + 2`
    - 使用更精确的可用空间计算，最大化第一页空间利用率
  - **优化效果**: 
    - 第一页空间得到最大程度利用，第3、4、5条甚至更多条款可以在第一页显示
    - 只有当条款确实在第一页显示不下时，才会移到第二页
    - 彻底避免第一页尾部出现不必要的空白
  - **影响范围**: 影响报价模块和订单确认模块PDF生成，最大化空间利用率
  - **向后兼容**: 优化不影响现有功能，仅改善分页逻辑的精确性

### 历史版本 V1.2.8 (2025-01-08) - Notes分页智能优化版本

**🔧 关键优化**
- ✅ **Notes条款分页阈值优化**: 优化分页检查的保守程度，让第一页空间得到更充分利用
  - **问题描述**: 之前的分页检查过于保守，导致第一页还有空白空间时就将后面的条款移到了第二页
  - **优化方案**: 调整分页检查的阈值和空间估算逻辑，让第一页空间得到更充分利用
  - **技术细节**: 
    - 将分页边界从`pageHeight - margin`改为`pageHeight - y - 30`，预留30mm给页码和底部边距
    - 减少条款高度估算中的额外间距，从`5 + 内容高度 + 5`改为`3 + 内容高度 + 3`
    - 使用更智能的可用空间计算，避免过早换页
  - **优化效果**: 
    - 第一页空间得到更充分利用，第3、4条可以在第一页显示
    - 只有当第5条确实在第一页显示不下时，才会移到第二页
    - 避免第一页尾部出现不必要的空白
  - **影响范围**: 影响报价模块和订单确认模块PDF生成，提升空间利用率
  - **向后兼容**: 优化不影响现有功能，仅改善分页逻辑的精确性

### 历史版本 V1.2.7 (2025-01-08) - Notes分页优化版本

**🔧 关键优化**
- ✅ **Notes条款分页逻辑优化**: 优化报价和订单确认模块的Notes条款分页逻辑，让分页位置更合理
  - **问题描述**: 之前的修复虽然解决了Notes条款被截断的问题，但分页位置不够合理，可能在条款中间截断
  - **优化方案**: 重新设计分页逻辑，允许在条款间截断，但确保单个条款完整显示
  - **技术细节**: 
    - 在渲染每个条款前检查剩余空间，确保整个条款能完整显示
    - 如果空间不足，将整个条款移到第二页，避免在条款中间截断
    - 允许在条款之间进行分页，充分利用第一页空间
  - **优化效果**: 
    - 第一页空间得到充分利用，避免不必要的空白
    - 条款内容完整显示，不会在中间被截断
    - 分页位置更加合理和美观
  - **影响范围**: 影响报价模块和订单确认模块PDF生成，提升Notes条款显示效果
  - **向后兼容**: 优化不影响现有功能，仅改善分页逻辑的合理性

### 历史版本 V1.2.6 (2025-01-08) - 报价模块Notes分页修复版本

**🐛 关键修复**
- ✅ **报价模块Notes条款分页修复**: 解决报价PDF中Notes条款过多时被第一页截断的问题
  - **问题描述**: 报价模块PDF生成时，当Notes条款内容较多时，无法正确显示到第二页，会被第一页截断
  - **根本原因**: Notes条款渲染时只在开始时检查一次分页，在渲染每个条款时没有检查是否需要换页
  - **修复方案**: 参考采购模块和订单确认模块的`checkAndAddPage`函数，为每个Notes条款添加分页检查
  - **技术细节**: 
    - 添加`checkAndAddPage`函数，在渲染每个条款前检查剩余空间
    - 估算每个条款所需空间（序号行 + 内容行数 * 行高 + 间距）
    - 确保条款内容不会被截断，自动换页显示
  - **影响范围**: 仅影响报价模块PDF生成，确保Notes条款完整显示
  - **向后兼容**: 修复不影响现有功能，仅改善Notes条款的显示完整性

- ✅ **订单确认模块Notes条款分页修复**: 解决订单确认PDF中Notes条款过多时被第一页截断的问题
  - **问题描述**: 订单确认模块PDF生成时，当Notes条款内容较多时，无法正确显示到第二页，会被第一页截断
  - **根本原因**: 订单确认模块虽然有`checkAndAddPage`函数，但在渲染Notes条款时没有使用分页检查
  - **修复方案**: 在Notes条款渲染循环中添加分页检查，确保每个条款都能完整显示
  - **技术细节**: 
    - 利用现有的`checkAndAddPage`函数，在渲染每个条款前检查剩余空间
    - 估算每个条款所需空间（编号行 + 内容行数 * 行高 + 间距）
    - 确保条款内容不会被截断，自动换页显示
  - **影响范围**: 仅影响订单确认模块PDF生成，确保Notes条款完整显示
  - **向后兼容**: 修复不影响现有功能，仅改善Notes条款的显示完整性

### 历史版本 V1.2.5 (2025-01-08) - 大写金额分页修复版本

**🐛 关键修复**
- ✅ **发票模块大写金额分页修复**: 解决发票PDF中大写金额在分页时丢失的问题
  - **问题描述**: 发票模块PDF生成时，当大写金额在第一页显示不下时会被截断或丢失
  - **根本原因**: 大写金额显示没有考虑分页情况，缺少分页检查逻辑
  - **修复方案**: 参考采购模块的`checkAndAddPage`函数，为大写金额添加分页检查
  - **技术细节**: 在`renderTotalAmount`函数中添加分页检查，确保大写金额完整显示
  - **影响范围**: 仅影响发票模块PDF生成，确保大写金额不会因分页而丢失
  - **向后兼容**: 修复不影响现有功能，仅改善大写金额的显示完整性

- ✅ **订单确认模块大写金额分页修复**: 解决订单确认PDF中大写金额在分页时丢失的问题
  - **问题描述**: 订单确认模块PDF生成时，当大写金额在第一页显示不下时会被截断或丢失
  - **根本原因**: 订单确认模块缺少`checkAndAddPage`函数，大写金额显示没有分页检查
  - **修复方案**: 添加`checkAndAddPage`函数，为大写金额添加分页检查
  - **技术细节**: 在订单确认PDF生成器中添加分页检查函数和大写金额分页逻辑
  - **影响范围**: 仅影响订单确认模块PDF生成，确保大写金额不会因分页而丢失
  - **向后兼容**: 修复不影响现有功能，仅改善大写金额的显示完整性

- ✅ **报价模块检查结果**: 报价模块没有大写金额显示功能，无需修复

**🔧 关键优化**
- ✅ **发票模块Other Fees显示修复**: 参考报价页面正确实现Other Fees的列合并显示
  - **问题描述**: 发票模块PDF生成时，Other Fees的序号不连续，且列合并方式不正确
  - **根本原因**: Other Fees行没有包含序号列，且没有正确合并中间列来显示费用描述
  - **修复方案**: 参考报价页面的实现方式，为Other Fees添加序号列并正确合并中间列
  - **技术细节**: 
    - 添加序号列：`(data.items.length + index + 1)`
    - 合并列数：`(data.showHsCode ? 1 : 0) + (data.showPartName ? 1 : 0) + (data.showDescription ? 1 : 0) + 3`
    - 费用描述显示在合并的列中，金额显示在Amount列
  - **影响范围**: 仅影响发票模块PDF生成，与报价页面保持一致的显示效果
  - **向后兼容**: 修复不影响现有功能，仅改善Other Fees的显示效果

**🔧 关键优化**
- ✅ **发票模块PDF分页优化**: 解决分页检查过于保守导致空白第二页的问题
  - **问题描述**: 发票模块PDF生成时，即使第一页还有足够空间，也会出现空白的第二页
  - **根本原因**: 分页检查预留空间过大（60mm、40mm、50mm），导致不必要的换页
  - **优化方案**: 实现智能分页检查，根据实际内容动态估算所需空间，避免过度保守的分页
  - **技术细节**: 在`invoicePdfGenerator.ts`中实现智能空间估算：总金额25mm、银行信息30mm、付款条款20mm、印章40mm
  - **优化效果**: 减少不必要的换页，提高PDF空间利用率，避免空白页面
  - **向后兼容**: 优化不影响现有功能，仅改善分页逻辑的精确性

- ✅ **发票模块PDF分页修复**: 解决表格占一页时后续条款无法正确显示到第二页的问题
  - **问题描述**: 当表格正好占满第一页时，后续的银行信息、付款条款等内容无法正确显示到第二页，可能被截断或显示不正确
  - **根本原因**: 发票模块在表格绘制完成后直接绘制后续内容，没有检查剩余空间是否足够
  - **修复方案**: 在关键内容绘制前添加分页检查逻辑，确保剩余空间不足时能够正确换页
  - **技术细节**: 在`invoicePdfGenerator.ts`中添加三处分页检查：表格后、总金额后、银行信息后，为后续内容预留足够空间
  - **影响范围**: 仅影响发票模块PDF生成，其他模块分页逻辑保持不变
  - **向后兼容**: 修复不影响现有功能，仅改善内容显示完整性

- ✅ **报价模块PDF分页修复**: 解决表格占第一页、Notes在第二页时页码识别错误的问题
  - **问题描述**: 当表格正好占满第一页，Notes需要换到第二页时，PDF显示"Page 1 of 1"而不是"Page 1 of 2"
  - **根本原因**: AutoTable的`didDrawPage`回调在表格绘制时立即计算页码，此时Notes部分还未执行`addPage()`
  - **修复方案**: 将页码计算延迟到所有内容（包括Notes）绘制完成后统一添加
  - **技术细节**: 修改`pdfTableGenerator.ts`中的`didDrawPage`回调，移除即时页码计算；在`quotationPdfGenerator.ts`中添加统一的`addPageNumbers`函数
  - **影响范围**: 仅影响报价模块PDF生成，其他模块页码计算逻辑保持不变
  - **向后兼容**: 修复不影响现有功能，仅改善页码显示准确性

### 历史版本 V1.2.0 (2025-01-08) - 基于0f53ddb4的稳定版本

**🎯 版本特点**
- ✅ **功能完整性**: 所有核心模块正常运行，无错误和警告
- ✅ **技术稳定性**: 基于稳定的提交版本，移除不必要的配置
- ✅ **性能优化**: 完整的性能优化和资源管理
- ✅ **用户体验**: 响应式设计，支持深色模式和主题切换

**🔧 技术栈状态**
- **前端框架**: Next.js 14 (App Router) - 稳定运行
- **UI组件**: Tailwind CSS - 完整主题系统
- **状态管理**: React Hooks + Zustand - 高效状态管理
- **部署平台**: 支持多种部署方式，专注核心功能

**📦 核心功能模块**
- **报价管理**: 完整的报价单创建、编辑、PDF导出、Excel导出
- **采购管理**: 供应商采购订单管理，支持多种采购流程
- **发票管理**: 发票生成和管理，支持多种发票类型
- **装箱单管理**: 详细的装箱清单，支持PDF导出
- **客户管理**: 客户和供应商信息管理，支持时间线跟踪
- **邮件系统**: AI邮件助手功能，支持多语言和多种风格
- **管理模块**: 用户权限管理，支持角色和权限控制
- **娱乐功能**: 五子棋和2048游戏，支持AI推演

**🚀 性能优化**
- **字体优化**: 中文字体压缩和预加载，提升加载速度
- **图片优化**: Logo和图标资源优化，减少加载时间
- **静态资源**: 资源嵌入和缓存优化
- **代码分割**: 懒加载和按需加载，提升首屏性能

**🛡️ 稳定性保障**
- **错误处理**: 完善的错误边界和异常处理机制
- **数据持久化**: 本地存储和状态管理
- **权限系统**: 完整的权限控制和用户管理
- **响应式设计**: 完美适配移动端和桌面端

### 历史版本 (2025-01-08)
- ✅ **Packing模块发票号重复记录修复**: 解决同一发票号多个记录的问题
  - **重复检查机制**: 采用与invoice模块相同的发票号重复检查逻辑
  - **数据唯一性**: 确保同一发票号自动合并到现有记录，避免数据重复
  - **业务逻辑统一**: 与invoice模块保持一致的保存和更新策略
  - **用户体验提升**: 避免重复数据造成的管理混乱，提升数据质量
  - **向后兼容**: 修复不影响现有数据的读取和显示
- ✅ **PDF表格渲染器**: 新增独立的表格渲染器，支持富文本中的表格显示
  - **表格渲染器**: 创建 `src/utils/tableRenderer.ts`，提供完整的表格解析和渲染功能
  - **富文本集成**: 在 `renderRichTextInPDF` 中拦截 `<table>` 节点，自动调用表格渲染器
  - **样式支持**: 支持表格边框、背景色、文本对齐、字体样式等完整样式
  - **合并单元格**: 支持 colspan 和 rowspan 合并单元格功能
  - **跨页支持**: 表格自动分页，支持跨页显示
  - **制表符转换**: 自动将制表符分隔的文本转换为HTML表格格式
  - **测试工具**: 提供 `testTableRenderer.ts` 测试文件，验证表格渲染功能
- ✅ **Packing模块Marks列换行功能**: 为packing模块的marks列添加换行支持
  - **换行支持**: 将marks列的input改为textarea，支持单元格内换行输入
  - **自动高度调整**: 单元格高度根据内容自动调整，最小高度28px
  - **多端支持**: 移动端卡片视图、桌面端表格视图、合并单元格模式全部支持
  - **样式统一**: 与描述列保持相同的换行样式和交互体验
  - **旧版本兼容**: 修改旧的packing组件，保持功能一致性
  - **PDF导出兼容**: PDF生成器已支持marks列的换行内容显示
- ✅ **Packing模块PDF表头统一**: 确保横向和纵向模式下的表头大小一致
  - **字体大小统一**: 横向和纵向模式都使用8pt字体，确保可读性
  - **内边距统一**: 横向和纵向模式都使用1mm内边距，保持一致性
  - **单元格高度统一**: 横向和纵向模式都使用相同的单元格高度
  - **表头文本统一**: 横向和纵向模式使用相同的表头文本，不简化标题
  - **代码简化**: 移除条件判断，使用统一的样式配置，提高可维护性
- ✅ **Packing模块PDF表头图片比例优化**: 修复横向和纵向模式下图片比例不一致的问题
  - **宽高比保持**: 正确计算和保持图片的原始宽高比，避免图片变形
  - **尺寸一致性**: 横向模式下保持与纵向模式相同的图片尺寸，避免图片过大
  - **居中显示**: 两种模式下图片都水平居中显示，视觉效果一致
  - **空间优化**: 横向模式下使用纵向标准宽度，纵向模式下充分利用垂直空间
- ✅ **Packing模块PDF表头文本换行优化**: 解决竖向模式下表头文本的换行问题
  - **括号换行修复**: 避免"N.W.(kg)"、"G.W.(kg)"等文本的括号被分割到不同行
  - **页面方向适配**: 竖向模式使用换行文本，横向模式保持单行文本
  - **文本布局优化**: 在括号前进行换行，确保括号完整性和语义正确性
  - **视觉效果提升**: 提高表头文本的可读性和专业外观
- ✅ **类型错误修复**: 修复构建过程中的TypeScript类型错误
  - **权限类型统一**: 修复 `PermissionMap` 接口中缺少 `confirmation` 字段的问题
  - **DocumentType定义统一**: 统一两个不同文件中的 `DocumentType` 定义顺序
  - **类型安全**: 确保 `accessibleDocumentTypes` 返回正确的 `DocumentType[]` 类型
  - **构建成功**: 修复后构建过程顺利完成，无类型错误
- ✅ **报价模块Excel导出功能**: 为销售确认页面添加Excel导出功能
  - **Excel导出服务**: 创建专门的Excel导出服务，支持报价单和销售确认两种格式
  - **销售确认优化**: 针对销售确认页面的特殊需求，提供专门的Excel模板
  - **完整数据导出**: 包含文档基本信息、商品明细、费用明细、总计金额和合同条款
  - **用户界面集成**: 在报价页面添加Excel导出按钮，支持快速导出
  - **文件命名规范**: 自动生成包含文档编号和日期的文件名
  - **错误处理**: 完善的错误处理和用户提示机制
- ✅ **PDF健康检查优化**: 优化开发环境PDF性能监控
  - **阈值调整**: 将健康检查阈值从800ms调整为2000ms，更符合实际性能表现
  - **日志分级**: 区分警告和错误级别，避免误报
  - **延迟优化**: 增加健康检查延迟时间，减少对首屏加载的影响
  - **性能监控**: 实时监控PDF生成性能，确保用户体验
- ✅ **主题管理系统**: 完整的主题管理解决方案
  - **CSS变量系统**: 统一管理颜色和样式，支持动态主题切换
  - **主题管理器**: 单例模式的主题管理类，支持配置持久化
  - **React Hooks**: 响应式的主题状态管理，提供便捷的操作方法
  - **主题组件**: 多种主题切换组件（按钮、下拉菜单、紧凑模式）
  - **主题提供者**: 应用根级别的主题上下文管理
  - **样式工具类**: 提供常用的主题样式组合和生成器
  - **深色模式**: 完整的深色模式支持，自动适配系统偏好
  - **模块化设计**: 主题系统完全模块化，易于扩展和维护
- ✅ **采购模块最终打磨**: 完成采购模块的最终优化和功能完善
  - **表单绑定升级**: 统一 `numberField`、`boolField`、`selectField` 输入处理，支持右对齐和兜底
  - **错误提示组件**: 字段下方小红标 + 区块汇总，直观定位错误信息
  - **自动保存**: 300ms 防抖 + 存储配额兜底，溢出时降级为压缩或仅保存关键字段
  - **PDF负载选择器**: 集中确定导出数据结构，避免页面到处拼装
  - **服务层健壮性**: 保存/加载异常的 UI 反馈（toast）与回退策略
  - **Store迁移规范**: 版本号与迁移脚手架固定模板，支持字段演进
  - **快捷操作增强**: 保存后轻量 toast + 键盘提示（⌘/Ctrl+S）
  - **验证逻辑优化**: 完善字段验证，支持实时错误提示
  - **用户体验提升**: 统一的错误处理、Toast提示、快捷键支持

### 历史版本 (2024-01-15)
- ✅ **PDF性能优化**: 实现字体和图片缓存系统，大幅提升PDF生成性能
  - **字体缓存**: 使用IndexedDB持久化字体字节，冷启动只解压一次
  - **图片缓存**: 图片转换为dataURL并缓存，避免重复解码
  - **表格优化**: 解决AutoTable宽度溢出问题，添加自适应列宽和长文本处理
  - **性能监控**: 新增性能监控工具，实时跟踪字体预热和PDF生成性能
  - **测试页面**: 新增性能测试页面，可验证缓存效果和性能指标
- ✅ **字体预热优化**: 首轮字体加载从9.7s优化到150-180ms
- ✅ **PDF生成优化**: 表格生成从463ms优化到250ms左右
- ✅ **缓存系统**: 支持字体和图片的版本控制和缓存管理
- ✅ **错误处理**: 完善的缓存错误处理和降级机制

### 历史版本
- ✅ **PDF字体性能重大优化**: IndexedDB缓存 + pako解压优化
  - **冷启动优化**: 9.7s → 预计2-3s，通过IndexedDB持久化字体字节
  - **热启动优化**: 150-180ms，IndexedDB缓存命中
  - **智能解压**: 优先使用浏览器原生DecompressionStream，兜底pako.ungzip
  - **容错回退**: .gz失败自动回退到.ttf，确保字体加载稳定性
  - **并发控制**: single-flight防止重复加载，避免资源浪费
  - **性能监控**: 详细的加载时间和缓存状态追踪
- ✅ **表格生成优化**: 自适应宽度 + 溢出处理
  - **列宽优化**: 根据页面可用宽度自动调整，避免内容溢出
  - **文本换行**: 长文本自动换行，软断点处理异常长词
  - **性能提升**: 表格生成从463ms优化到预计<250ms
- ✅ **Dashboard 页面重构**: 模块化拆分，提升代码可维护性
- ✅ **性能优化**: 统一文档计数逻辑，使用 Map 加速权限判断
- ✅ **搜索功能**: 支持文档编号、客户名称搜索，关键词高亮显示
- ✅ **用户体验**: 响应式筛选器，智能空状态提示
- ✅ **代码质量**: 完整的 TypeScript 类型定义，完善的错误处理
- ✅ 新增2048游戏AI推演功能
- ✅ 实现Expectimax算法和蒙特卡洛树搜索
- ✅ 添加随机演示功能，用于调试和蒙特卡洛模拟
- ✅ 添加推演统计和速度控制
- ✅ 支持手动干预和撤销功能
- ✅ 优化移动端用户体验
- ✅ 修复状态同步问题，使用JSON深拷贝确保状态隔离
- ✅ 增强AI算法性能，支持Alpha-Beta剪枝优化
- ✅ 添加调试日志，便于问题排查
- ✅ 修复重新开始功能数据恢复问题，确保重新开始后刷新页面不会恢复老数据
- ✅ 增强全局粘贴功能，新增支持"名称，数量，单位，单价"4列格式智能识别，修复单价为0时的识别问题

## 贡献指南

欢迎提交Issue和Pull Request来改进项目。

## 许可证

MIT License
ok